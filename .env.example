# docker compose --profile app up -d için. Kopyalayıp .env olarak kaydedin; .env repoya girmez.
# Üretimde en az 32 baytlık rastgele bir değer kullanın: openssl rand -base64 48
JWT_SECRET=degistir-beni-en-az-32-bayt-uzunlugunda-bir-gizli-anahtar
POSTGRES_PASSWORD=lifequest
# Açılışta admin rolü verilecek hesap (kayıttan sonra geçerli olur)
ADMIN_EMAIL=admin@lifequest.local
